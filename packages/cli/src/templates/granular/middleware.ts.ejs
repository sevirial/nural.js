import { defineMiddleware } from "@nuraljs/core";

export const <%= camelName %>Middleware = defineMiddleware(async (req) => {
  // 💡 Your Logic Here
  const header = req.headers["x-custom-header"];

  // Return data to extend the request context
  return {
    <%= camelName %>Data: header || "default"
  };
});
